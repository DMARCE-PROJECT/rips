#!/bin/rips

levels:
	ALEV; #A level
	B;
	C soft;
consts:
	potato int= 12;
vars:
	nmsg int = 0;
	another int = 0;

rules Msg:
	 true ?
		set(nmsg, nmsg + 1), alert(CurrRule), True(nmsg);
	 true ?
		set(another, potato + 1), alert(CurrRule), True(another);
	false ?
		trigger(B), trigger(C);
