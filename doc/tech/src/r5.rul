levels:
	__DEFAULT__;
	HALT;

consts:

vars:

rules Msg:

	plugin("/var/tmp/plugin.py") && CurrLevel != HALT ?
		alert("plugin detected a bad message"),
		exec("/usr/bin/spd-say", "plugin detected a bad message"),
		exec("/bin/sleep", "5"),
		trigger(HALT);
